FROM surrealdb/surrealdb:v3
# All features native — no extensions needed.
# SurrealDB includes relational, graph, vector (HNSW),
# timeseries, JSON/schema-less, and full-text search natively.
# Note: This is a scratch-based image (no shell).
# Volume permissions are handled via docker-compose user directive.
